import './globals.css';

export const metadata = {
  title: 'Active Recall là gì? | Học hiệu quả',
  description:
    'Giải thích chi tiết về phương pháp Active Recall và cách ứng dụng để học tập hiệu quả hơn.'
};

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  );
}
